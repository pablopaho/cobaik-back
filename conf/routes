# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Bikes
GET     /bikes                      co.com.akku.bikes.BikesController.bikes
POST    /insert/bike                co.com.akku.bikes.BikesController.insertBike

#Silhouette
OPTIONS /*path                                                                  controllers.ApplicationController.options(path)

GET     /api/v1/authenticate/:provider                                          co.com.wesurance.users.silhouette.SocialAuthController.authenticate(provider)
POST    /api/v1/authenticate/:provider                                          co.com.wesurance.users.silhouette.SocialAuthController.authenticate(provider)
